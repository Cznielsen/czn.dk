# New boring czn.dk
server {

  listen 80;
  server_name czn.dk;

  location / {
    root   /usr/share/nginx/html;
    index  index.html index.htm;
    try_files $uri $uri/ /index.html;
  }

  error_page   500 502 503 504  /50x.html;

  location = /50x.html {
    root   /usr/share/nginx/html;
  }

}

# Original czn.dk server Duane
server {

  listen 80;
  server_name og.czn.dk;

  location / {
    root /var/www/orig-czndk/czndk;
    index index.html;
    try_files $uri $uri/ /index.html;
  }

  error_page   500 502 503 504  /50x.html;

  location = /50x.html {
    root   /var/www/orig-czndk/czndk;
  }
}

# Original czn.dk pepe banner
server {

  listen 80;
  server_name cs.czn.dk;

  location / {
    root /var/www/orig-czndk/cs;
    index index.html;
    try_files $uri $uri/ /index.html;
  }

  error_page   500 502 503 504  /50x.html;

  location = /50x.html {
    root   /var/www/orig-czndk/cs;
  }
}